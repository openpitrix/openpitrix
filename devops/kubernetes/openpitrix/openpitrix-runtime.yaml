apiVersion: v1
kind: Service
metadata:
  name: openpitrix-runtime-manager
  namespace: openpitrix-system
  labels:
    app: openpitrix
    service: openpitrix-runtime-manager
spec:
  selector:
    app: openpitrix
    service: openpitrix-runtime-manager
  ports:
  - name: openpitrix-runtime-manager
    protocol: TCP
    port: 9103
    targetPort: 9103
---
apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: openpitrix-runtime-manager
  namespace: openpitrix-system
  labels:
    app: openpitrix
spec:
  selector:
    matchLabels:
      app: openpitrix
  replicas: 1
  template:
    metadata:
      labels:
        app: openpitrix
        service: openpitrix-runtime-manager
    spec:
      initContainers:
      - name: wait-mysql
        image: busybox
        imagePullPolicy: IfNotPresent
        command: ['sh', '-c', 'until nc -z openpitrix-db.openpitrix-system.svc.cluster.local 3306; do echo "waiting for mysql"; sleep 2; done;']
      - name: wait-etcd
        image: busybox
        imagePullPolicy: IfNotPresent
        command: ['sh', '-c', 'until nc -z openpitrix-etcd.openpitrix-system.svc.cluster.local 2379; do echo "waiting for etcd"; sleep 2; done;']
      containers:
      - name: runtime-manager
        image: openpitrix/openpitrix:latest
        imagePullPolicy: Always
        command:
          - runtime-manager
        ports:
        - containerPort: 9103
          name: runtime-manager
        env:
        - name: OPENPITRIX_LOG_LEVEL
          value: ${OPENPITRIX_LOG_LEVEL}
        - name: OPENPITRIX_MYSQL_DATABASE
          value: "runtime"
        - name: OPENPITRIX_MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              key: password.txt
              name: mysql-pass
        resources:
          limits:
            cpu: 500m
            memory: 500Mi
          requests:
            cpu: 500m
            memory: 500Mi

