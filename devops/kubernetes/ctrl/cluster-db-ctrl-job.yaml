apiVersion: batch/v1
kind: Job
metadata:
  annotations:
  labels:
    app: openpitrix-cluster-db-ctrl
  name: openpitrix-cluster-db-ctrl
  namespace: default
spec:
  activeDeadlineSeconds: 600
  backoffLimit: 6
  completions: 1
  parallelism: 1
  template:
    metadata:
      labels:
        job-name: openpitrix-cluster-db-ctrl
      name: cluster-db-ctrl
    spec:
      containers:
      - command: ["flyway", "-X", "-url=jdbc:mysql://openpitrix-db.default.svc.cluster.local/cluster", "-user=root", "-password=password","-validateOnMigrate=false", "-locations=filesystem:/flyway/sql/cluster", "migrate"]
        env:
        - name: FLYWAY_PASSWORD
          valueFrom:
            secretKeyRef:
              key: password.txt
              name: mysql-pass
        image: openpitrix/openpitrix:flyway
        imagePullPolicy: IfNotPresent
        name: cluster-db-ctrl
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      nodeSelector:
        role: master
      restartPolicy: OnFailure
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
      tolerations:
      - effect: NoSchedule
        key: node-role.kubernetes.io/master
        operator: Exists
